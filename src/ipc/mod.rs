// IPC: Unix socket transport (abstracted for future TCP).

pub fn connect() {
    // Phase 3+: connect to server Unix socket.
    todo!("ipc::connect");
}

pub fn listen() {
    // Phase 3+: bind server Unix socket.
    todo!("ipc::listen");
}
