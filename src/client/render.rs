// Client render: compute viewport, diff-based escape-sequence output.

pub fn render() {
    // Phase 2+: diff-based rendering of the grid to the local terminal.
    todo!("client::render::render");
}
