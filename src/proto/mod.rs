// Protocol: client↔server message protocol (encode/decode).

pub fn encode() {
    // Phase 3+: length-prefixed binary message encoding.
    todo!("proto::encode");
}

pub fn decode() {
    // Phase 3+: length-prefixed binary message decoding.
    todo!("proto::decode");
}
